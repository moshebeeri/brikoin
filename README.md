#Brikoin

This project implements Realestate trading in fractions using Ethereum, React, and Firebase.


## How to:

- install: `yarn`
- run: `yarn dev`
- build: `yarn build`
- deploy: `yarn deploy`

